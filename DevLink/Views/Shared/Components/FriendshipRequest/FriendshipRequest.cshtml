@model string

@if (Model == "unauthorized")
{
    <script>
        window.location.href = '@Url.Action("LogIn", "Auth")';
    </script>
}
else
{
    <span class="font-weight-bold ml-auto mr-3">@Model</span>
}